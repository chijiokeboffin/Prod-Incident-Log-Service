spring:
  application:
    name: customer
  datasource:
    password: password
    url: jdbc:postgresql://db:5432/incidentreport
    username: postgres
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true


server:
  port: 8080
  error:
    include-binding-errors: always
    include-message: always

jwt:
  token:
    secret: UEBzc3dvcmQxMCQ=

